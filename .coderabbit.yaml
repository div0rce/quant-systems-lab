# yaml-language-server: $schema=https://storage.googleapis.com/coderabbit_public_assets/schema.v2.json

reviews:
  pre_merge_checks:
    docstrings:
      mode: "off"
  tools:
    fbinfer:
      enabled: false
